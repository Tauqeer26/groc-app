import 'package:flutter/material.dart';

class CustReg extends StatelessWidget {
  const CustReg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.blue,
    );
  }
}